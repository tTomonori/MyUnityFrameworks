using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaygroundMain : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //vector書き込み
        //Dictionary<string, object> tDic = new Dictionary<string, object>();
        //tDic.Add("enum", Direction.up);
        //tDic.Add("vector2", new Vector2(100, 55.66f));
        //tDic.Add("vector3", new Vector3(10.2f, 10.3f, 10.4f));
        //MyJson.serializeToFile(tDic, "Assets/resources/jsontest.json",true);

        //vector読み込み
        //Dictionary<string, object> tDic = MyJson.deserializeFile("Assets/resources/jsontest.json");
        //MyDebugLog.Log(tDic["vector2"]);
        //MyDebugLog.Log(tDic["vector3"]);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
